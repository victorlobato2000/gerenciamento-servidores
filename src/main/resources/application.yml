spring:
  datasource:
    password:
    url: jdbc:mysql://localhost:3306/servidordb
    username: root
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        format_sql: true
    show-sql: true